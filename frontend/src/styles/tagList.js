const margins = {
  xl: 16,
  xs: 8,
};

const paddings = {
  xl: '14px 28px',
  xs: '7px 14px',
};

const fontSizes = {
  xl: 28,
  xs: 14,
};

const tagList = {
  margins,
  paddings,
  fontSizes,
};

export default tagList;
